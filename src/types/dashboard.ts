export type StatCard = {
  title: string;
  value: string;
  growth: string;
  icon: string;
  iconBg: string;
};

export type Activity = {
  id: number;
  name: string;
  action: string;
  time: string;
  image: string;
};